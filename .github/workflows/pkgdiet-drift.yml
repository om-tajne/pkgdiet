name: PkgDiet Weekly Drift Scan
on:
  schedule:
    - cron: '0 0 * * 1' # Every Monday at 00:00
jobs:
  drift-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install -g pkgdiet
      - run: pkgdiet drift
