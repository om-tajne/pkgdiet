# Continue Integration (Planned)

PkgDiet is planning an integration with Continue. You can currently add the PkgDiet MCP server to your `config.json` via the UI, but native skill blocking is still being validated.
